<div class="table_pane">
  <h1>Recipes</h1>
  
  <% if user_signed_in? && current_user.admin? %>
  <%= link_to 'Add Recipe', new_recipe_path, class: "green button" %>
  <div class="space_16"></div>
  <% end %>

  <table id="recipes_table" class="stripe">
    <thead>
      <tr>
        <th>Name</th>
        <th>CO2e (kg) per serving</th>
      </tr>
    </thead>
  </table>
</div>

<div id="current_recipe" class="content_pane">
  <% if @recipe %>
  <%= render 'show' %>
  <% else %>
  <h1>Your favourite dish: Climate Sinner or Winner?</h1>
  
  <p style="font-size:16px">
  Find some of the most common dinner dishes and their ingredients list. Based on the ingredients list we have calculated the CO2-emissions for you to check if your favourite dish is a climate sinner or a climate winner.
  </p>
  <p style="font-size:16px">
  Soon it will also be possible to find the instruction for the recipes and be inspired not only to try out new interesting recipes, but also find alternatives to high-emitting dishes. 
  <p>
  <% end %>
</div>

<div style="clear:both;"></div> 
